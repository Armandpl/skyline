# @package _global_

defaults:
  - override /model: r18_longi

dataset:
  train:
    root_dir: "../../trajectory_optimization/data/longi_max5_400k"
model_artifact_name: null # edited from model:v8 to match the longi network
epochs: 20
